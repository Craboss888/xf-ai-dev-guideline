#!/bin/bash
# cc-preflight · 每轮自检 hook（用作 Claude Code 的 stop hook）
# 每轮回复结束时打印一份自检清单，逼自己别陷入"代码跑通但没解决问题"。
# 配置方法见同目录 README.md。

cat <<'EOF'
【每轮自检 · cc-preflight】
1. 核心难点是用「实际运行结果」验证过的吗？还是只是代码跑通了？
2. 还在最初的验收标准上吗？优先级对吗？还剩多少时间？
3. 这一轮有要记的关键结论 / 踩坑吗？→ 写进开发日志（重要的进 CLAUDE.md）。
4. 有没有因为缺权限 / 卡点在绕路？→ 停下来问我。
EOF
